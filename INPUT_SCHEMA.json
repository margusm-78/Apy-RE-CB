{
  "title": "Coldwell Banker Jacksonville Agents Scraper (Optimized v3)",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "editor": "requestListSources",
      "description": "Agent city pages or office pages to start from.",
      "prefill": [
        {
          "url": "https://www.coldwellbanker.com/city/fl/jacksonville/agents"
        },
        {
          "url": "https://www.coldwellbanker.com/fl/jacksonville/agents"
        }
      ]
    },
    "listPageCount": {
      "title": "City list pages to scan (fallback)",
      "type": "integer",
      "minimum": 1,
      "default": 25,
      "description": "If we can't detect the last page from the DOM, we'll enqueue pages 2..N using this number (e.g., 25)."
    },
    "maxPages": {
      "title": "Max list pages (upper bound)",
      "type": "integer",
      "minimum": 1,
      "default": 60,
      "description": "Safety cap. Numeric pagination won't enqueue beyond this."
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "minimum": 1,
      "default": 4,
      "description": "Keep modest on small containers."
    },
    "maxAgents": {
      "title": "Stop after N agents",
      "type": "integer",
      "minimum": 1,
      "default": 200,
      "description": "Soft cap to avoid run timeouts. Increase if you raise the run timeout."
    },
    "blockResources": {
      "title": "Block heavy resources",
      "type": "boolean",
      "default": true,
      "description": "Block images, fonts, media, stylesheets and common analytics domains to speed up crawling."
    },
    "followContact": {
      "title": "Follow Contact page if email missing",
      "type": "boolean",
      "default": true,
      "description": "If true, enqueue the agent's Contact page when email is not found on profile."
    },
    "proxy": {
      "title": "Proxy configuration",
      "type": "object",
      "editor": "proxy",
      "description": "Use Apify Proxy or your own proxy."
    }
  },
  "required": [
    "startUrls"
  ]
}